'use strict';

var tutils = require('./tutils');
var _describe = tutils._describe;
//var _it = tutils._it;
//var bup = tutils.bup;

_describe('checkevent', function() {
	// Empty for now (umpires may not need to check this after all)
});
