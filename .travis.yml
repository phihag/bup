dist: trusty
sudo: false

language: node_js
node_js:
  - 6
  - node
env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8

install:
  - make deps
before_script:
  - make lint
notifications:
  email:
    - phihag@phihag.de

